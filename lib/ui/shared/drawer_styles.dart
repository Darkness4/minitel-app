import 'package:flutter/material.dart';

class DrawerStyle {
  static const RoundedBorder = BorderRadius.only(
    bottomRight: Radius.circular(40.0),
    topRight: Radius.circular(40.0),
  );
  static const HightlightMargin = EdgeInsets.only(right: 8.0);
}
